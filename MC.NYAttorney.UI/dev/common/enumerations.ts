export enum enmStatus {
    Active, Inactive, Disabled, Deleted, Locked
}

export enum enmTabs {
    OrderSummary,
    OrderDetail,
    PropertyDetail,
    OrderTitle,
    TitleClearance,
    PreClose,
    PostClose,    
    OrderDocuments,
    OrderNotes
}
